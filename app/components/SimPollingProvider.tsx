"use client";

import { ReactNode } from 'react';

/**
 * Minimal SimPollingProvider for opensource version
 * This is a placeholder - the opensource version doesn't need polling
 */
export function SimPollingProvider({ children }: { children: ReactNode }) {
  return <>{children}</>;
}

